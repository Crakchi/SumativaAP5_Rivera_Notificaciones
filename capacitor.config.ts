import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SumativaAP5_Rivera_Notificaciones',
  webDir: 'www'
};

export default config;
